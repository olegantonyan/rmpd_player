import { h } from 'hyperapp'

export default ({ state, actions }) =>
  <div id="settings">
    <div class="header">
      <h1>Settings</h1>
    </div>

    <div class="content">
      <p>Form with wifi, playser, other settings</p>
    </div>
  </div>
