{
  "name": "webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --no-source-maps --no-cache",
    "prebuild": "rm -rf dist",
    "deploy": "rm -rf ../fs/*.{ico html css js}; cp -R dist/* ../fs/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "hyperapp": "^1.2.9",
    "parcel-bundler": "^1.10.3",
    "src": "^1.1.2"
  },
  "devDependencies": {
    "babel-core": "^6.26.3"
  }
}
